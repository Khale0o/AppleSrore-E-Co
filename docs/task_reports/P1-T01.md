# P1-T01 Report

## Status

Completed; manual runtime checks remain pending

## Files created or modified

- Replaced `lib/main.dart` with the production bootstrap entry point.
- Created production application, router, theme/token, accessibility, design-system, and placeholder-route files under `lib/app/`, `lib/core/`, and `lib/features/`.
- Replaced root spike tests with production-foundation route and Reduced Motion tests.
- Updated `pubspec.yaml` and `pubspec.lock` for approved dependencies.

## Dependencies added and why

- `flutter_riverpod: ^3.3.2` for the global Reduced Motion state contract, scoped provider access, and testable overrides without code generation.
- `go_router: ^17.3.0` for named declarative routes, path parameters, error handling, and future deep-link readiness.

## Application bootstrap approach

`main.dart` initializes Flutter and delegates to `bootstrap()`, which wraps `AppleStoreApp` in `ProviderScope`. No storage, APIs, assets, Firebase, or asynchronous startup work is initialized.

## Router structure

Named routes: `/`, `/home`, `/catalog`, `/product/:productId`, `/product/:productId/configure`, and `/cart`, plus a themed error destination. Top-level routes use `goNamed`; details/configuration use `pushNamed` to preserve Back behavior.

## Theme and state decisions

- Dark-only semantic graphite/silver/cool-blue theme with system typography, compact spacing/radius tokens, and restrained duration tokens.
- Reduced Motion effective value is `development override ?? platform preference`; it is not persisted and has no code generation.

## Tests and validation

- Explicit test coverage: Splash start; Splash-to-Home; Home-to-Catalog; Home-to-sample Details; supplied `productId` display; Details-to-Configuration with preserved `productId`; Cart route; themed unknown-route error page; Reduced Motion effective override; and navigation while Reduced Motion is enabled.
- Final test count: 10 passed.
- `dart format .`: passed; 31 Dart files checked.
- `flutter analyze`: passed; no issues found.
- `flutter test`: passed; 10 tests passed.
- `git diff --check`: passed. Git reported non-blocking LF-to-CRLF working-copy warnings for `lib/main.dart`, `pubspec.lock`, and `test/widget_test.dart`.
- No emulator, device, ADB, or `flutter run` command was run during this correction.

## Issues

- The previous emulator launch attempt is a historical execution mistake and was not repeated. It does not establish a Flutter environment failure; manual runtime observation remains pending for the user.

## TODOs

- Replace placeholder screens only in later approved feature tasks.
- Add persistence for Reduced Motion only if a later approved task justifies `shared_preferences`.
- The user should manually verify the placeholder route graph, Back behavior, Reduced Motion navigation, no overflow, and that the production app no longer launches the spike.

## Spike isolation

The P0-T08 source remains unchanged under `lib/spikes/carousel_hero/`. The production entry point and production routes do not import spike code.

## Static review confirmation

- Production `lib/` code does not import spike code; the spike remains isolated under `lib/spikes/carousel_hero/`.
- Only `flutter_riverpod` and `go_router` were added. No persistence, external assets, Firebase, networking, or final feature UI was added.
- Product IDs are preserved from Details to Configuration, and unknown routes use the themed error page.
