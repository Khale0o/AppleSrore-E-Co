# Deferred Features

The following are explicitly deferred: backend and Firebase; authentication; remote product API; checkout implementation; orders; notifications; admin panel; fake 3D image sequence; fragment shaders; advanced comparison; full search and filters; full ecosystem implementation; store release configuration; and final application identifiers.

Deferred features must not be implemented unless a later approved task explicitly activates them.

